export enum ProjectPageType {
  Projects = "projects",
  Personal = "personal",
}

export enum ProjectSortOrder {
  DateDesc = "date-desc",
  DateAsc = "date-asc",
  NameAsc = "name-asc",
  NameDesc = "name-desc",
}